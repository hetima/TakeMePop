<UserControl x:Class="Listhing.Views.Controls.ModalOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Listhing.Views.Controls"
             xmlns:res="clr-namespace:Listhing"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    
     <UserControl.Resources>
        <!-- フェードインアニメーション -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="OverlayGrid"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- フェードアウトアニメーション -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="OverlayGrid"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0"
                           Duration="0:0:0.15">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- コンテンツのスケールアニメーション -->
        <Storyboard x:Key="ContentScaleInStoryboard">
            <DoubleAnimation Storyboard.TargetName="ContentScaleTransform"
                           Storyboard.TargetProperty="ScaleX"
                           From="0.9" To="1"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ContentScaleTransform"
                           Storyboard.TargetProperty="ScaleY"
                           From="0.9" To="1"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="OverlayGrid" Opacity="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <!-- 半透明カバー -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                MouseLeftButtonDown="Overlay_MouseLeftButtonDown"/>

        <!-- コンテンツエリア -->
        <Border Grid.Row="1"
                x:Name="ContentBorder"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                MaxWidth="700"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.RenderTransform>
                <ScaleTransform x:Name="ContentScaleTransform" ScaleX="0.9" ScaleY="0.9" CenterX="0" CenterY="0"/>
            </Border.RenderTransform>
            
            <Grid KeyboardNavigation.TabNavigation="Cycle">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- ヘッダー（タイトルと閉じるボタン） -->
                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Button Grid.Column="1"
                            Width="24"
                            Height="24"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Padding="0"
                            Click="CloseButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            ToolTip="{x:Static res:Strings.ModalCloseButtonTooltip}">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="14"
                                   Text="&#xE106;"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Button>
                </Grid>
                <!-- コンテンツ -->
                <ContentControl Grid.Row="1" x:Name="ModalContentControl"
                                Content="{Binding}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
